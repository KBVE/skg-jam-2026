// Mirrors the authoritative values in godot/scripts/game/config.gd.
// Only what the HUD needs to render (e.g. the time-bar max).
export const BASE_TIME = 60;
export const TIME_PURCHASE_COST = 100;
export const TIME_PURCHASE_SECONDS = 60;
